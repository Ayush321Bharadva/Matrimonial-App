import 'package:flutter/material.dart';
import 'package:myproject/pages/crud_api.dart';

class APIUserDetail extends StatelessWidget {
  const APIUserDetail({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('User Detail'),
      ),
      body: Center(
        child: Column(
          children: [
            // Text(${}),
          ],
        ),
      ),
    );
  }

}
